package www.senthink.com.micro.test;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import www.senthink.com.micro.test.chache.mapCache;

/**
 * Created by hyacinth on 2017/12/1.
 */
public class DateUtilTest {

    private final static Logger LOGGER = LoggerFactory.getLogger(DateUtilTest.class);

    public static void main(String[] args){
        mapCache cache = new mapCache();
        /*MailClientUtil mail = new MailClientUtil();
        mail.setMailClient();*/
        /*Long time = 0l;
        if (time.equals(0l)) {
            LOGGER.debug("equal");
        }else {
            LOGGER.debug("not equal");
        }
        cache.add("010203", 12l);
        Long timeOne = cache.getTimeOne("010203");
        Long gpsTime = cache.getGpsTime("010203");
        LOGGER.debug("timeOne={}", timeOne);
        LOGGER.debug("gpsTime={}", gpsTime);*/
    }

}
